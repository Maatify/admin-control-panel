{% extends "layouts/base.twig" %}

{% block title %}Admins{% endblock %}

{% block content %}
<div class="container-fluid">
    {# Header #}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Admins</h1>
    </div>

    <div id="alert-container"></div>

    <p class="text-muted">Manage system administrators</p>

    {# Search Section #}
    <div class="card mb-4">
        <div class="card-body">
            <form id="admins-search-form" class="row g-3">
                {# Global Search #}
                <div class="col-md-12">
                    <label for="search-global" class="form-label">Global Search</label>
                    <input type="text" class="form-control" id="search-global" placeholder="Search ID or Email...">
                </div>

                {# Column Filters #}
                <div class="col-md-3">
                    <label for="filter-id" class="form-label">Admin ID</label>
                    <input type="text" class="form-control" id="filter-id" placeholder="Filter by ID">
                </div>
                <div class="col-md-3">
                    <label for="filter-email" class="form-label">Email</label>
                    <input type="text" class="form-control" id="filter-email" placeholder="Filter by Email">
                </div>

                {# Date Range #}
                <div class="col-md-3">
                    <label for="date-from" class="form-label">Created From</label>
                    <input type="date" class="form-control" id="date-from">
                </div>
                <div class="col-md-3">
                    <label for="date-to" class="form-label">Created To</label>
                    <input type="date" class="form-control" id="date-to">
                </div>

                <div class="col-md-12 d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary me-2">Search</button>
                    <button type="button" class="btn btn-secondary" id="btn-reset">Reset</button>
                </div>
            </form>
        </div>
    </div>

    {# DataTable Section #}
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover" id="admins-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Email</th>
                            <th>Created At</th>
                        </tr>
                    </thead>
                    <tbody>
                        {# Content loaded via API #}
                        <tr>
                            <td colspan="3" class="text-center">Loading...</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            {# Pagination #}
            <nav aria-label="Admins pagination" class="d-flex justify-content-between align-items-center mt-3">
                <div class="d-flex align-items-center">
                    <select class="form-select form-select-sm me-2" id="per-page-select" style="width: auto;">
                        <option value="10">10</option>
                        <option value="20">20</option>
                        <option value="50">50</option>
                        <option value="100">100</option>
                    </select>
                    <div class="text-muted small" id="pagination-info">
                        Showing 0 to 0 of 0 entries
                    </div>
                </div>
                <ul class="pagination mb-0" id="pagination-controls">
                    {# Pagination links generated by JS #}
                </ul>
            </nav>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script src="/assets/js/pages/admins-list.js"></script>
{% endblock %}
