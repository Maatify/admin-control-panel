{% extends "layouts/base.twig" %}

{% block title %}Admins{% endblock %}

{% block content %}
<div class="container-fluid">
    {# Header #}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Admins</h1>
    </div>

    <div id="alert-container"></div>

    <p class="text-muted">Manage system administrators</p>

    {# Search Section #}
    <div class="card mb-4">
        <div class="card-body">
            <form id="admins-search-form" class="row g-3">
                <div class="col-md-6">
                    <label for="filter-search" class="form-label">Search</label>
                    <input type="text" class="form-control" id="filter-search" placeholder="Search by ID or Email">
                </div>
                <div class="col-md-6 d-flex align-items-end">
                    <button type="submit" class="btn btn-primary me-2">Search</button>
                    <button type="button" class="btn btn-secondary" id="btn-reset">Reset</button>
                </div>
            </form>
        </div>
    </div>

    {# DataTable Section #}
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover" id="admins-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Email</th>
                            <th>Created At</th>
                        </tr>
                    </thead>
                    <tbody id="admins-table-body">
                        {# Content loaded via API #}
                        <tr id="loading-row">
                            <td colspan="3" class="text-center">Loading...</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            {# Empty State (Hidden by default) #}
            <div id="empty-state" class="text-center py-5 d-none">
                <h4 class="text-muted">No admins found</h4>
                <p class="text-secondary">Try adjusting your search criteria.</p>
            </div>

            {# Error State (Hidden by default) #}
            <div id="error-state" class="text-center py-5 d-none">
                <h4 class="text-danger">Error loading admins</h4>
                <p class="text-secondary">Please try refreshing the page.</p>
            </div>

            {# Pagination #}
            <nav aria-label="Admins pagination" class="d-flex justify-content-between align-items-center mt-3" id="pagination-container">
                <div class="d-flex align-items-center">
                    <select class="form-select form-select-sm me-2" id="per-page-select" style="width: auto;">
                        <option value="10">10</option>
                        <option value="20">20</option>
                        <option value="50">50</option>
                        <option value="100">100</option>
                    </select>
                    <div class="text-muted small" id="pagination-info">
                        Showing 0 to 0 of 0 entries
                    </div>
                </div>
                <ul class="pagination mb-0" id="pagination-controls">
                    {# Pagination links generated by JS #}
                </ul>
            </nav>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script src="/assets/js/admins-list.js"></script>
{% endblock %}
